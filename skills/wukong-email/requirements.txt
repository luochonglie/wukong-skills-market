# Wukong Email - Python dependencies
# =============================
# Only a Markdown library is needed (for auto-conversion when sending email)

# Markdown to HTML (used when sending email)
markdown>=3.10
